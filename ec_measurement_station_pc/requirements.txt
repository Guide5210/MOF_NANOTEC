# ต้องมีจริงตอนนี้
pyserial>=3.5          # ต่อพอร์ตจอ P4 · ตรวจว่าพอร์ตไหนเป็นอะไร
matplotlib>=3.5        # กราฟในหน้า viewer  (P1-B)

# tkinter ไม่ได้อยู่ใน pip — มากับตัวติดตั้ง Python บน Windows อยู่แล้ว
# ถ้าไม่มี ให้ติดตั้ง Python ใหม่แล้วติ๊ก "tcl/tk and IDLE"

# ยังไม่ต้องการ (และจะไม่ต้องการใน P1)
# openpyxl / reportlab — การออกรายงานเป็นของระบบเดิมทั้งหมด
